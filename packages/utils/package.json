{
    "name": "@stock-rec/utils",
    "version": "1.0.0",
    "description": "Shared utilities for the stock recommendation system",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "lint": "eslint src/**/*.ts",
        "type-check": "tsc --noEmit"
    },
    "dependencies": {
        "@stock-rec/types": "workspace:*",
        "aws-lambda": "^1.0.7",
        "zod": "^3.22.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.0",
        "typescript": "^5.0.0"
    }
}